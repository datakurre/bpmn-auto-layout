{
  "$schema": "https://github.com/datakurre/bpmn-auto-layout/schemas/bpmn-layout-rules.v1.json",
  "artifact": "bpmn-layout-rules",
  "version": 1,
  "status": "managed implementation-derived artifact",
  "source": {
    "implementation": [
      "packages/bpmn-auto-layout/src/auto-layout.ts",
      "packages/bpmn-auto-layout/src/element-dimensions.ts",
      "packages/bpmn-auto-layout/src/graph-analysis.ts",
      "packages/bpmn-auto-layout/src/node-placement.ts",
      "packages/bpmn-auto-layout/src/channel-planning.ts",
      "packages/bpmn-auto-layout/src/edge-routing.ts",
      "packages/bpmn-auto-layout/src/collision-repair.ts",
      "packages/bpmn-auto-layout/src/label-placement.ts",
      "packages/bpmn-auto-layout/src/di-creation.ts",
      "packages/bpmn-auto-layout/src/label-layout.ts"
    ],
    "extraction_method": "Manual extraction from current TypeScript behavior; every rule below is labelled implementation-derived and cites the implementation lines used.",
    "determinism": "Do not include timestamps. Update only when current TypeScript behavior or measured feedback-loop metrics change."
  },
  "rules": [
    {
      "id": "layout.defaults.grid-and-tracks",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/element-dimensions.ts:34-44", "packages/bpmn-auto-layout/src/node-placement.ts:21-42"],
      "rule": "Default layout uses a 120 px semantic column pitch, a 10 px diagram-js-compatible center grid, spine Y=70, lower tracks at Y=180 and Y=430, 140 px track gap, and nominal channels at Y=140/280/560 before dynamic channel planning."
    },
    {
      "id": "layout.dimensions.by-type",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/element-dimensions.ts:49-55", "packages/bpmn-auto-layout/src/node-placement.ts:570-630"],
      "rule": "Events are 36x36, gateways 50x50, and all other non-subprocess node types default to 100x80. Expanded subprocess container dimensions are computed dynamically from a recursive child layout: the children's bounding box plus SUBPROCESS_PAD_H=40px on each side and SUBPROCESS_PAD_V=30px on each side, with minimums of 360x200."
    },
    {
      "id": "layout.input.cross-container-sequence-flows",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/graph-analysis.ts", "packages/bpmn-auto-layout/src/auto-layout.ts:39"],
      "rule": "Sequence flows whose source or target belongs to a different immediate process/subprocess container are rejected before layout; existing BPMN diagrams are then removed and regenerated."
    },
    {
      "id": "layout.spine.selection",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/node-placement.ts:206-242", "packages/bpmn-auto-layout/src/node-placement.ts:303-328"],
      "rule": "A DFS marks back-edges as loop-backs. The primary spine starts at the first start event and follows non-back-edge outgoing flows, scoring end events high, subprocesses and reject/error/fail/give-up branches low, and looping targets lower than completion exits. During branch-track assignment, named exception branches normally use the upper track, except terminal exception branches targeting an end event, which use the lower track so a gateway can fan out horizontally and downward."
    },
    {
      "id": "layout.branch.tracks-and-columns",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/node-placement.ts:244-423", "packages/bpmn-auto-layout/src/node-placement.ts:653-665"],
      "rule": "Spine nodes occupy increasing columns with a 50 px edge clearance derived from actual node widths. When lane membership is declared, top-level lane members prefer deterministic tracks based on lane order; otherwise branches are placed breadth-first relative to their parent. Exception branches go upward, ordinary non-spine branches go downward unless a loop-back spans the gateway, same-track nodes are shifted fractionally to avoid width-based overlap without introducing a whole-column gap, and direct same-track terminal flows are repaired to the same clearance after packing. buildTrackColMapsWithDim accepts effective dimensions so recursively sized subprocesses drive column/track spacing."
    },
    {
      "id": "layout.spacing.default-flow-gap",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/element-dimensions.ts:57-58", "packages/bpmn-auto-layout/src/node-placement.ts:284-288"],
      "rule": "Sequential flow-node paths use a uniform 50 px edge-to-edge clearance, matching the default horizontal spacing used by bpmn-js for newly placed flow nodes. Node widths contribute half-widths on both sides of each gap."
    },
    {
      "id": "layout.tracks.dynamic-clearance",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/node-placement.ts:651-673"],
      "rule": "When an upper track exists, it becomes the visual spine; lower tracks are offset from the default 140 px track pitch. Tracks containing nodes taller than 80 px add cumulative vertical clearance."
    },
    {
      "id": "layout.subprocess.children",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/node-placement.ts:444-519", "packages/bpmn-auto-layout/src/node-placement.ts:570-735"],
      "rule": "Expanded subprocesses are laid out recursively. computeProcessLayout is called on the subprocess flowElements in local coordinates; the resulting bounding box plus SUBPROCESS_PAD_H=40 px left/right and SUBPROCESS_PAD_V=30 px top/bottom determines the container size (minimum 360x200). After the outer layout finalizes parent positions, layoutSubProcessChildrenRecursive translates child node layouts to absolute coordinates in the parent layoutNodes map with isSubProcessChild=true, preserving grid-sized translations and centered content when the minimum container is larger than the child bounds."
    },
    {
      "id": "layout.boundary-events.attachment",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/node-placement.ts:309-325", "packages/bpmn-auto-layout/src/node-placement.ts:598-682", "packages/bpmn-auto-layout/src/edge-routing.ts:138-155", "packages/bpmn-auto-layout/src/di-creation.ts:470-476"],
      "rule": "Boundary events are positioned on their attached host's top or bottom boundary. Multiple boundary events on one host alternate sides deterministically. Outgoing branch target activities without competing incoming flows are shifted to the upper track; traversal continues from each shifted target so its downstream nodes, including terminal end events, inherit the branch track and remain near the branch. The leftmost exception target may align to the primary outgoing target column while later branch targets retain their columns. Routes first leave through the attached side before using an orthogonal channel and enter upper-track targets through their top boundary, preventing sequence flows from crossing the host body."
    },
    {
      "id": "labels.boundary-events.outward",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/label-placement.ts:416-470"],
      "rule": "Boundary-event labels prefer the side away from their attached host, trying centered and deterministic lateral offsets before falling back to normal event-label placement. This keeps labels from being printed inside the host activity while preserving collision-aware selection."
    },
    {
      "id": "layout.collaboration-and-lane-di",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/auto-layout.ts:44-57", "packages/bpmn-auto-layout/src/di-creation.ts:28-37", "packages/bpmn-auto-layout/src/di-creation.ts:40-380"],
      "rule": "When a bpmn:Collaboration exists, processes referenced by participants are excluded from standalone per-process BPMNDiagram generation. createCollaborationDi emits a single BPMNDiagram containing: participant pool shapes with isHorizontal=true sized from content bounding box plus PAD_LEFT=60, PAD_RIGHT=30, PAD_TOP=30, PAD_BOTTOM=30 (participants stacked with 40 px gaps); lane shapes with isHorizontal=true; all flow-node shapes and sequence-flow edges from each participant process (shifted to absolute coordinates via dx/dy); and named message-flow edges. Message flows are routed through the same repairSegmentCollisions pipeline sequence flows use: a candidate 4-point mid-line route is repaired against a collaboration-wide obstacle set (every participant's flow nodes, merged container boundary strips, and every already-placed sequence- and message-flow route), then validated, falling back to the unrepaired candidate if repair does not validate. Standalone processes (not in any collaboration) receive per-process BPMNDiagram with lane shapes derived from member node bounds."
    },
    {
      "id": "layout.components.pack-independent",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/node-placement.ts:45-177", "packages/bpmn-auto-layout/src/node-placement.ts:731-733"],
      "rule": "Disconnected top-level components are packed after local layout, preferring a compact row below the main start-event component when their total width is at most 75% of the main component, otherwise using the landscape A4 aspect-ratio score. Node center X values are first snapped to the 75 + n*120 semantic grid when the move is at most 2 px, then all node centers are quantized to the configured 10 px compatibility grid."
    },
    {
      "id": "layout.grid.compatibility",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/node-placement.ts:17-42", "packages/bpmn-auto-layout/src/di-creation.ts:23-48"],
      "rule": "Final node centers are quantized independently on X and Y using gridSize, defaulting to 10 px to match diagram-js grid snapping. Intermediate orthogonal route points are quantized when the resulting polyline remains orthogonal; shape-boundary attachment points remain determined by node geometry."
    },
    {
      "id": "routing.channel-planning",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/channel-planning.ts:14-119", "packages/bpmn-auto-layout/src/edge-routing.ts:196-209"],
      "rule": "Loop-back and bypass channels are derived from the occupied node band with 30 px clearance. Channel planning is container-scoped (planContainerScopedChannels): nodes are grouped by their immediate container -- the top-level diagram, or one expanded subprocess's own children -- and each group's band and lane assignment is computed independently, so a subprocess-internal channel is bounded by that subprocess's own content, never by the outer diagram's. Overlapping channel spans within a container are assigned separate 30 px lanes using a two-pass greedy plan before final waypoints are emitted. Same-track gateway bypasses with an upper-track branch use an upper channel so they do not cross that branch; other same-track bypasses use the lower channel."
    },
    {
      "id": "routing.objective-policy",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/layout-policy.ts:6-16", "packages/bpmn-auto-layout/src/layout-policy.ts:18-66", "packages/bpmn-auto-layout/src/auto-layout.ts:1962-1973"],
      "rule": "Visibility-route candidates are filtered for orthogonality and minimum-turn constraints, then selected deterministically by the configured turn preference, path length, and serialized-point tie-breaker. The default policy requires orthogonal routes, permits zero minimum turns, prefers fewer turns, and imposes no additional path-length factor."
    },
    {
      "id": "routing.waypoint-cases",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/edge-routing.ts:123-460"],
      "rule": "Waypoint routing handles boundary-event departures, gateway-to-gateway upside bypasses, loop-backs, same-track forward flows, gateway branches up/down, same-track gateway bypasses through dedicated channels, downward exception routes around intervening content, upper-track exits to end events, merges into gateways, and a final orthogonal fallback. These same cases route subprocess-internal (child-to-child) flows too, not a separate stub -- obstacle scans that exclude subprocess children (the short-circuit direct check, the same-track forward-flow check, and the downward gateway-branch check) are container-scoped via inSameContainer, so an internal flow is only obstructed by its own container's siblings and an external flow is unaffected."
    },
    {
      "id": "routing.repair-and-fanout",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/collision-repair.ts:72-100", "packages/bpmn-auto-layout/src/collision-repair.ts:136-185", "packages/bpmn-auto-layout/src/collision-repair.ts:432-558", "packages/bpmn-auto-layout/src/process-routing.ts:61-247", "packages/bpmn-auto-layout/src/lane-layout.ts:159-222"],
      "rule": "Crowded node-side attach points are fanned out deterministically by flow id. Routed polylines are repaired by enforcing outward departure, treating nearby nodes, protected central portions of every existing path, and pool/lane container boundary obstacles as obstacles, trying visibility routes and ordered 15 px nudges up to 300 px, and side-approaching blocked final legs. External routes treat expanded subprocesses as opaque obstacles, while internal subprocess flows route against sibling content. Container boundary obstacles are thin strips -- pool top/bottom/right borders, the pool name-label gutter (solid), and lane dividers -- computed from lane/participant band geometry once known and passed into repairSegmentCollisions as `layout.containerObstacles`; the lane/pool interior itself is not an obstacle. Before serialization, every sequence flow must attach to both node boundaries, depart and arrive orthogonally outward, and use a source and target side compatible with clearly dominant horizontal or vertical separation; invalid restoration or fallback routes are rejected. Only validated gateway channel routes may be reasserted after this pass; non-gateway routes are never overwritten after validation."
    },
    {
      "id": "routing.deterministic-order",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/process-routing.ts:31-53", "packages/bpmn-auto-layout/src/process-routing.ts:61-121"],
      "rule": "Both the channel-span-recording pass and the post-repair edge-vs-edge avoidance pass iterate flows in orderFlowsForRouting's order -- ascending structural span (column + track distance between endpoints), then source id, then target id -- instead of source-document order, so inserting an unrelated sequence flow earlier in the XML cannot renumber or re-route flows that were already fine. The post-repair pass also skips re-running repair on a route that already has zero obstacle hits (countRouteHits) and passes validateConnectionPoints, so a route is never perturbed by an obstacle set that changed after it was already correct."
    },
    {
      "id": "architecture.routing-extracted-from-di",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/process-routing.ts:1-266", "packages/bpmn-auto-layout/src/di-creation.ts:415-424"],
      "rule": "routeProcessFlows (process-routing.ts) is the complete sequence-flow routing pipeline -- channel planning, seven-case waypoint computation, collision repair, orthogonalization, and the deterministic ordering above -- as a pure function from a ProcessLayoutResult to a Map of finished waypoints, with no dependency on moddle or DI shape construction. di-creation.ts's buildProcessShapesAndEdges calls it once per process/participant and translates the result into bpmndi:BPMNEdge elements; it no longer computes routes itself.",
      "note": "This is a deliberately scoped extraction, not the full architectural split proposed in #31: label placement (edge and node labels) and DI-shape construction still live in di-creation.ts, ProcessLayoutResult does not yet carry finished label bounds or per-flow waypoints as part of computeProcessLayout's own return value, and a collaboration's per-participant waypoints are still computed once per participant in already-shifted coordinates rather than computed once and translated. Routing was extracted first because it was the largest, most self-contained, and least XML-dependent piece, and because #10/#26/#30's fixes already made it a well-isolated function of (layout, opts) alone."
    },
    {
      "id": "routing.avoids-labels",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/di-creation.ts:772-826"],
      "rule": "After every node and edge label is placed, each sequence flow's final route is re-checked against every *other* placed label (excluding its own edge label and its own source/target node labels) using the same obstacle/hit-counting machinery repairSegmentCollisions already uses. A route that crosses one is re-repaired with those labels added as one-off obstacles, but the repaired route is adopted only if it both reduces label hits and does not increase shape/edge hits against the original (label-free) obstacle set -- §8 ranks no-overlap above label avoidance, so this pass may trade a label crossing for nothing worse, never for a new or larger shape/edge crossing.",
      "note": "This is deliberately reactive (fix confirmed crossings after labels exist) rather than reserving speculative label space before every route is chosen. The speculative-reservation approach was implemented and rejected during development: it perturbed the candidate route search broadly enough that a route which had cleanly detoured around an expanded subprocess started cutting through it and one of its children instead, in a fixture with no label anywhere near that detour. The current pass is more conservative and only found no candidate for a handful of already-crowded label/edge crossings, which the label-search-widening and hard-constraint work (#16) also could not resolve for the same crowded positions."
    },
    {
      "id": "feedback.geometry-diagnostics",
      "label": "quality-gate",
      "source_lines": ["tools/bpmn_feedback.py:923-952", "tools/bpmn_feedback.py:1078-1184", "tools/bpmn_feedback.py:1545-1580"],
      "rule": "The feedback report records the exact edge pairs, edge/shape pairs, edge/subprocess-container pairs, and invalid endpoint attachments responsible for geometry defects. It also records invalid label bounds. The quality gate fails on any such defect while preserving details in metrics.json and command output so routing regressions can be traced to named BPMN elements."
    },
    {
      "id": "api.best-effort-with-warnings",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/layout-warnings.ts:1-32", "packages/bpmn-auto-layout/src/auto-layout.ts:1-80", "packages/bpmn-auto-layout/src/process-routing.ts:169-260", "packages/bpmn-auto-layout/src/label-placement.ts:259-340", "packages/bpmn-auto-layout/src/label-placement.ts:501-690"],
      "rule": "The engine always produces a renderable diagram rather than throwing on unsatisfiable geometry (#32's decision). routeProcessFlows no longer throws when no candidate route validates even after every fallback; it emits the best available geometry and a ROUTE_INVALID_CONNECTION_POINTS warning. Three previously-silent degradation sites now report structured LayoutWarning objects (code, optional elementId, message) instead of degrading unnoticed: repairSegmentCollisions' best-effort result is checked once, after routing, for remaining obstacle hits (ROUTE_INTERSECTS_OBSTACLE); pickLabel's combined-score fallback (ROUTE crossing an edge, or overlapping a shape/label with no clean candidate) (LABEL_OVERLAPS_ELEMENT); and solveLabelPlacement's edge-collision-relaxed, containment-relaxed, and unchecked-default tiers (LABEL_OVERLAPS_ELEMENT / LABEL_ESCAPES_CONTAINER). layoutProcessWithDiagnostics (auto-layout.ts, exported from index.ts) returns `{xml, warnings}`; layoutProcess keeps its original string-only signature and simply does not collect them -- both call the same internal runLayout, verified to produce byte-identical XML.",
      "note": "bpmn_feedback.py's `check` command does not yet consume these warnings (the issue's fourth suggested step) -- that requires the layout-command CLI defined in flake.nix to surface them in a machine-readable form (e.g. a sidecar file), which is outside packages/bpmn-auto-layout and was left as a follow-up."
    },
    {
      "id": "architecture.priority-model",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/layout-policy.ts:1-52", "tools/bpmn_feedback.py:1949-2020"],
      "rule": "The §8 priority model is represented as LAYOUT_PRIORITY_LEVELS (layout-policy.ts), an ordered, named, documented list of the seven levels, plus comparePriorityViolations, a lexicographic comparison primitive: a violation at a lower-numbered level always outranks any number of violations at higher-numbered levels. `bpmn-feedback check` tags every failure line with the priority level (METRIC_PRIORITY_LEVEL) its metric is evidence for and sorts output by level, so a failing run surfaces its most important defects first instead of in metric-declaration order. choosePreferredRoute (routing.objective-policy) is the one existing candidate-selection pass documented as encoding two of the seven levels (3 and 6).",
      "note": "This is infrastructure and reporting only, not the full retrofit the issue describes: no candidate-producing pass other than choosePreferredRoute has been rewritten to report its violations against the ladder and let comparePriorityViolations choose, and packIndependentComponents' aspect-ratio compactness scoring (§8 level 7) can still decide component placement ahead of left-to-right structural concerns (level 4) -- the issue itself asks for a design discussion before that retrofit, which this change does not attempt to resolve unilaterally."
    },
    {
      "id": "labels.node-placement",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/label-placement.ts:430-530", "packages/bpmn-auto-layout/src/label-placement.ts:283-425"],
      "rule": "Named events and gateways receive external BPMNLabel bounds. Candidate label positions vary by flow attachment directions, gateway shape, preferred top/bottom side, text width, and line count; single-line primary gateway labels align their top edge with a 4 px gap, while multi-line primary labels shift upward so their bottom edge aligns with the single-line label row. Snug candidates keep a 6 px horizontal clearance. When a selected gateway candidate needs multiple lines to avoid path or element collisions, the formatted line break is serialized into the element name so renderers display the intended rows. Candidates are rejected, in order of decreasing priority, for: escaping the node's own container, colliding with a routed edge, colliding with an element, or colliding with a previously placed label; each constraint is relaxed only if no candidate satisfies it and every stricter constraint before it."
    },
    {
      "id": "labels.node-gap-by-element-class",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/element-dimensions.ts:66-79", "packages/bpmn-auto-layout/src/label-placement.ts:460-475"],
      "rule": "The label solver uses an 8 px primary/alternate vertical gap for named events (NODE_LABEL_GAP) and a 6 px primary/alternate vertical gap for gateways (GATEWAY_LABEL_GAP), with a 6 px snug-corner gap (SNUG_LABEL_GAP) and a 2 px edge-label gap (EDGE_LABEL_GAP); collision-free candidate selection can choose a different side or offset. These four gaps are named constants in element-dimensions.ts rather than repeated local literals, so \"labels shall use consistent default gaps\" (§4) is checkable. This is a generalized parameterized heuristic, but the desired gateway-above versus gateway-below preference is not yet a complete formal rule."
    },
    {
      "id": "labels.edge-placement",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/label-placement.ts:339-450"],
      "rule": "Named sequence flows receive labels from candidates generated over every eligible horizontal segment (>= 30 px) on the route, longest first, or otherwise a single vertical segment (>= 20 px) if no horizontal segment qualifies. Horizontal labels are centered on each segment's own midpoint with a 2 px above/below clearance depending on whether another route runs close above. A label's center may slide at most LABEL_SLIDE_SLACK (15 px) past either end of the segment it's centered on. Edge labels are placed before node labels so node-label placement can avoid them."
    },
    {
      "id": "labels.edge-crossing-priority",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/label-placement.ts:252-330"],
      "rule": "pickLabel treats crossing another route as a harder constraint than shape/label overlap: among the crossing-free candidates (if any exist), it picks the one with the least shape/label overlap, returning immediately on zero overlap. Only when every candidate crosses some route does it fall back to minimizing a single combined score (shape/label overlap area plus 120 per crossing), so the result is still the least-bad option rather than an arbitrary one.",
      "note": "In fixtures where every reachable candidate across every eligible segment already crosses something (dense gateway/loop areas), this still degrades to the combined-score fallback -- the constraint is enforced, but a genuinely saturated diagram has no crossing-free position to find."
    },
    {
      "id": "labels.subprocess-obstacles",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/label-placement.ts:93-121", "packages/bpmn-auto-layout/src/label-placement.ts:268-278", "packages/bpmn-auto-layout/src/label-placement.ts:365-373"],
      "rule": "Every label-collision predicate (labelCollidesWithElements, pickLabel's overlap scoring, computeEdgeLabelBounds' straddled-segment scan) treats expanded-subprocess children as ordinary collision obstacles. The subprocess container itself is skipped as an obstacle only via isOwnContainer -- when the label's own owner (the node, or the flow's source node) is a child of that specific subprocess -- so a label may sit near or inside the container that legitimately owns it, but a label belonging to an unrelated node must still avoid the container's box and everything inside it."
    },
    {
      "id": "labels.container-containment",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/label-placement.ts:151-163", "packages/bpmn-auto-layout/src/label-placement.ts:563-599", "packages/bpmn-auto-layout/src/di-creation.ts:416-428", "packages/bpmn-auto-layout/src/lane-layout.ts:159-174"],
      "rule": "solveLabelPlacement and computeEdgeLabelBounds reject any candidate not fully contained (containedIn) by the label's own owning container -- di-creation.ts's containerBoundsFor resolves this per node as its subprocess if it is a child, else its leaf lane band (leafLaneBoundsByNodeId), else its participant pool, else no constraint for a standalone process with no lanes. Containment is enforced ahead of edge-collision avoidance and is relaxed only as a last resort, after every candidate fails it, so a label degrades to escaping its container rather than failing to render. The `cand.x < 10 || cand.y < 0` canvas-origin clamp still applies underneath this as a final absolute floor.",
      "note": "A label's owning node can itself be drawn outside its declared lane band by a separate, pre-existing node-placement bug (see node_containment_violations); when that happens no candidate near the node can satisfy lane containment either, and the fallback tier is used."
    },
    {
      "id": "labels.message-flow-and-data-reference",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/di-creation.ts:344-372", "packages/bpmn-auto-layout/src/di-creation.ts:728-742"],
      "rule": "Message-flow labels are computed by computeEdgeLabelBounds against the collaboration-wide node map and every already-placed sequence-flow, message-flow, and (for a message flow) other message-flow label, with no container-containment constraint since a message flow spans two pools by definition. Data-object/data-store reference labels are computed by solveLabelPlacement the same way an event/gateway label is, including the node's own container containment. Every label from both paths is pushed into the relevant placedLabels list so later labels see it too -- neither path emits an unchecked fixed box any more."
    },
    {
      "id": "labels.ensure-default-di",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/label-layout.ts:77-116"],
      "rule": "ensureLabelDi materializes default BPMNLabel rectangles for named events, gateways, and lanes when shape DI exists but label bounds do not; event/gateway defaults are 90x20 below the shape, and lane defaults are a 30 px tall rectangle rotated into the lane boundary."
    },
    {
      "id": "labels.validation",
      "label": "implementation-derived",
      "source_lines": ["packages/bpmn-auto-layout/src/label-layout.ts:156-244"],
      "rule": "labelLayout reports missing label DI for named events/gateways/lanes, invalid label bounds, lane labels outside lane bounds, labels overlapping elements or labels, and labels outside containing lanes or participants."
    }
  ],
  "metrics": [
    {
      "id": "metric.semantic.element_counts",
      "producer": "tools/bpmn_feedback.py report",
      "definition": "Counts BPMN XML elements by local name plus sequence flows, message flows, lanes, and data references for fixture coverage and regression slicing."
    },
    {
      "id": "metric.layout.canvas",
      "producer": "tools/bpmn_feedback.py report",
      "definition": "Computes diagram extents from shape bounds and edge waypoints as x, y, width, and height."
    },
    {
      "id": "metric.layout.overlaps",
      "producer": "tools/bpmn_feedback.py report",
      "definition": "Counts non-container shape overlaps, total shape overlap area, and label-label overlaps from BPMN DI bounds."
    },
    {
      "id": "metric.routing.quality",
      "producer": "tools/bpmn_feedback.py report",
      "definition": "Counts edge crossings, edge intersections with non-endpoint non-container shapes, route intersections with expanded subprocess, lane, and participant containers that neither endpoint belongs to, invalid source/target boundary attachments, total bends, Manhattan length, and non-orthogonal segments."
    },
    {
      "id": "metric.routing.lattice",
      "producer": "tools/bpmn_feedback.py report",
      "definition": "Checks every orthogonal edge segment against the 50 px default route unit, reporting non-multiple segments and the largest distance from a 50 px multiple. A route may contain multiple-unit segments and turns."
    },
    {
      "id": "metric.spacing.horizontal_flow_gap",
      "producer": "tools/bpmn_feedback.py report",
      "definition": "Measures edge-to-edge gaps for same-track, left-to-right sequence-flow endpoints with no intervening non-container shape, reporting count, minimum, maximum, and average gap in pixels. Longer bypass and loop flows are excluded."
    },
    {
      "id": "metric.grid.center_x_deviation",
      "producer": "tools/bpmn_feedback.py report",
      "definition": "Measures non-container shape center-X deviation from the implementation grid 75 + n*120, reported as average and maximum absolute deviation."
    },
    {
      "id": "metric.labels.named_label_coverage",
      "producer": "tools/bpmn_feedback.py report",
      "definition": "Compares named events, gateways, lanes, sequence/message flows, and data references against BPMN DI labels, reporting covered and missing label targets."
    },
    {
      "id": "metric.labels.geometry-diagnostics",
      "producer": "tools/bpmn_feedback.py report",
      "definition": "Records invalid label bounds plus label intersections with non-own shapes and non-own routes. Container bounds that contain a label are treated as valid containment rather than a collision. These diagnostics guide label-solver work and remain informational until the persisted fixture baseline is collision-free."
    },
    {
      "id": "metric.labels.node_label_gap",
      "producer": "tools/bpmn_feedback.py report",
      "definition": "Reports count, minimum, maximum, and average vertical gap between named event/gateway shapes and their BPMNLabel bounds, grouped by element class. Use this to compare gateway labels with the 8 px event-label baseline."
    }
  ],
  "feedback_observations": [
    {
      "id": "feedback.order-fulfilment-advanced.label-spacing",
      "status": "accepted implementation behavior",
      "source": "order-fulfilment-advanced.png",
      "observation": "The overall three-row grid, happy-path spine, branch spacing, event-subprocess alignment, and label-to-node distances are good. The exclusive gateway label 'Order valid?' could use a slightly larger below-node gap; the above-node labels 'Check in parallel' and 'Checks complete' could use a small consistent gap comparable to event labels.",
      "next_step": "Continue visual review with another single label-placement target if needed."
    }
  ],
  "feedback_ingestion": {
    "schema": "bpmn-layout-feedback/v1",
    "loop": [
      "Generate or provide BPMN fixtures.",
      "Run the report command to capture original/transformed images and deterministic metrics.",
      "Collect feedback JSON with checkbox choices, Likert ratings, and comments.",
      "Map low ratings and selected choices to this ruleset and the report metrics.",
      "Change TypeScript behavior, update this artifact from the changed implementation, and rerun targeted reports."
    ]
  }
}
